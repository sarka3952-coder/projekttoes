version: '3.8'
services:
  quiz-app:
    build: .
    ports:
      - "8081:8081"
    environment:
      - APP_PORT=8081
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
